<div class="archive-main-card">
    <div class="archive-top">
      <div class="main-title-bar">
        <div class="main-title-dot"></div>
        <h1 class="article-title">资料</h1>
      </div>    
    </div>
    <% 
        function build_tags_string(tags) {
            let str = "";
            for(let tag of tags.data) {
                str += "#" + tag.name + " ";
            }
            return str.trim();
        }

        // 筛选type为resource的文章
        let resourcePosts = site.posts.filter(function(post) {
            return post.type === 'resource';
        }).sort('date', -1);

        let countByYear = {};
        resourcePosts.each(function(post, i) {
            let year = post.date.year();
            countByYear[year] = countByYear[year] ? countByYear[year] + 1 : 1;
        })

        function getCountText(year) {
            let count = countByYear[year];
            if (count == 1)
                return "1 项资料";
            else
                return count + " 项资料";
        }
    %>
    <% let currYear = -1; %>
    <% resourcePosts.each(function(post, i) { %>
      <% let year = post.date.year(); %>
      <% if (year != currYear) { %>
        <% currYear = year; %>
        <div class="year-line">
            <div class="year-num"><%- year %></div>
            <div class="dot-wrapper">
                <div class="dot"></div>
            </div>
            <div class="article-count"><%- getCountText(year) %></div>
        </div>
      <% } %>
      <a class="archive-article-link" href="<%- url_for(post.path) %>" title="<%- post.title %>">
        <div class="date"><%- post.date.format("MM[-]DD") %></div>
        <div class="line">
            <div class="dot"></div>
        </div>
        <div class="title">
            <div class="title-inner">
                <% if (post.title) { %>
                    <%- post.title %>
                <% } else { %>
                    未命名
                <% } %>
            </div>
        </div>
        <div class="tags">
            <%- build_tags_string(post.tags) %>
        </div>
      </a>
    <% }) %>
  </div>
